// const Workout = require('../data/Workout')
//
// const pageSize = 10
//
// module.exports = class WorkoutsService {
//   static getMyWorkouts (filters, sorting, page) {
//     return Workout.find({filters}).sort(sorting).skip(page * pageSize).limit(pageSize)
//   }
// }